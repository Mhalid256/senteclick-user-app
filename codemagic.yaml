workflows:
  default-workflow:
    name: Default Workflow
    environment:
      flutter: stable
      java: 17
      android_signing:
        - keystore
    scripts:
      - flutter clean
      - flutter pub get
      - flutter build appbundle --release --no-tree-shake-icons
    artifacts:
      - build/**/outputs/**/*.aab